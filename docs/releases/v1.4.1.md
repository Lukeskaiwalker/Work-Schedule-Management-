# v1.4.1 - Admin update flow release-metadata sync patch

Date: 2026-03-05

## Highlights

- Keeps runtime release labels synchronized across all update paths:
  - admin manual update command list now includes `./scripts/update_release_metadata.sh`,
  - admin automatic update install flow now runs `./scripts/update_release_metadata.sh` right after `git pull`.
- Preserves `v1.4.0` feature set (materials catalog expansion, DATANORM workflows, office/report UX updates, and time-input normalization).

## Upgrade Notes

- Recommended:
  - `BACKUP_PASSPHRASE='<passphrase>' ./scripts/safe_update.sh --pull --branch main`
- If using admin in-app update flow:
  - release metadata refresh is now built into the generated/manual command sequence.

## Validation Baseline

- `docker compose run --rm --build api sh -lc 'cd /app && PYTHONPATH=. pytest -q tests/test_auth_rbac.py -k "update_status or install_update"'` passes.
- `./scripts/test.sh` passes (`66 passed` + web build pass).
