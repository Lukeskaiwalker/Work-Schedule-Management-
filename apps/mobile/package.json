{
  "name": "smpl-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Native iOS shell for the SMPL Workflow web app (Capacitor).",
  "scripts": {
    "build:web": "npm --prefix ../web run build",
    "sync": "npm run build:web && cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/keyboard": "^8.0.5",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
