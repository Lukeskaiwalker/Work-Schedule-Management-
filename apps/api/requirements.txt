fastapi==0.116.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.43
psycopg2-binary==2.9.10
alembic==1.16.5
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
pydantic-settings==2.10.1
python-multipart==0.0.20
httpx==0.28.1
cryptography==45.0.7
python-dotenv==1.1.1
pytest==8.4.2
pytest-cov==6.2.1
sse-starlette>=1.6.1
asyncpg>=0.29.0

email-validator==2.2.0
bcrypt==3.2.2
reportlab==4.2.5
openpyxl==3.1.5
Pillow==11.3.0
pillow-heif==1.2.1

# v2.4.0: OpenAI Structured Outputs for line-item extraction.
# 1.55+ has native Structured Outputs support via the chat.completions
# `response_format={"type": "json_schema"}` shape.
#
# 1.55.3 specifically: fixes "Client.__init__() got an unexpected keyword
# argument 'proxies'" — OpenAI's HTTP client passed proxies= which
# httpx 0.28+ removed. v1.55.3 release notes:
#   "fix(client): respect new httpx behavior of removing default kwargs"
# Without this pin, every line-item extraction job dies with that
# TypeError on the v1.55.0 + httpx 0.28.1 combo we previously had.
openai==1.55.3
# pdf2image needs the system poppler-utils binary (added to the API
# Dockerfile in the same commit). Pillow is already pulled as the
# imaging backend.
pdf2image==1.17.0
